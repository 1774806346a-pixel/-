import type { ShotGroup } from "../../domain/models";
export function ShotGroupWorkspace({ groups, onExport }: { groups: readonly ShotGroup[]; onExport?: (group: ShotGroup) => void }) { return <section className="shots-workspace" aria-label="分镜组"><div className="section-heading"><strong>分镜组</strong><span>{groups.length} 组 · 每组 10 秒</span></div>{groups.map((group) => <article className="shot-group-row" key={group.id}><div><strong>{group.id}</strong><span>{group.shots.length} 个 timed shots · {group.sceneInfo}</span></div><p>{group.videoPrompt}</p><ol>{group.shots.map((shot) => <li key={shot.shotId}>{shot.timing.start.toFixed(2)}-{shot.timing.end.toFixed(2)}s · {shot.shotSize} · {shot.visualAction}</li>)}</ol><button type="button" onClick={() => onExport?.(group)}>复制本组提示词</button></article>)}</section>; }

