# AI 漫剧剧本工作台：产品验收、开发标准与流程

## 1. 文档目的

本文是 AI 漫剧剧本工作台的产品验收规范，也是研发、测试和发布的共同基准。所有功能需求必须能够追溯到：

`需求编号 -> 开发任务 -> 测试用例 -> 验收证据 -> 发布结论`

本文适用于本地运行、无用户登录、支持 Ollama 和用户自定义 API 的 V1 完整工作台。V1 不包含云端协作、账号体系和内置图像/视频生成服务。

## 2. 产品目标与边界

### 2.1 产品目标

帮助 AI 漫剧创作者完成：

`一句话创意/已有剧本 -> 结构化解析 -> 目标导向诊断 -> 可控改写 -> 剧本定稿 -> 分镜拆解 -> 资产与生成提示词导出`

### 2.2 评价原则

- “评分”是基于题材、受众、时长和创作目标的诊断，不宣称绝对客观。
- AI 输出必须可解释、可编辑、可撤销、可重新生成。
- 用户锁定的人物、世界观和剧情事实默认不可被无提示地改写。
- 项目数据默认保存在本机；用户 API Key 不进入项目文件、导出文件或日志。
- 先保证结构化中间数据和连续性，再追求一次性生成的完整度。

### 2.3 V1 非目标

- 用户注册、登录、云同步和多人协作。
- 平台代用户购买或托管模型额度。
- 内置图片、视频、配音或音乐生成服务。
- 把模型生成结果直接认定为最终可拍摄成片。

## 3. 角色与核心对象

- **创作者**：输入创意/剧本、设定创作目标、确认修改和导出结果。
- **项目**：一个独立创作空间，包含原稿、设定、版本、评分、分镜、资产和提示词。
- **故事圣经**：项目级人物、世界观、时间线、场景和道具的锁定事实。
- **剧本版本**：原稿或一次 AI/人工修改后的完整可恢复版本。
- **分镜组**：按集、场、镜头组织的可生成视频镜头数据。
- **模型配置**：Ollama 或用户自定义 API 的地址、模型、参数及安全引用。

## 4. 验收等级与判定

- **P0**：主流程、数据安全和数据完整性；任一失败不得进入内测。
- **P1**：影响效率、质量解释性和连续性的功能；内测前应通过，特殊情况需产品负责人书面接受风险。
- **P2**：增强体验或高级导出；可排入后续版本，但必须记录状态。

严重级别：

- **S0**：数据丢失、密钥泄露、无法启动、任意用户输入导致崩溃；立即阻断发布。
- **S1**：核心流程不可完成、结果与输入严重不符、无法恢复；阻断发布。
- **S2**：局部功能错误但存在可用替代路径；修复后发布或明确已知问题。
- **S3**：视觉、文案或非关键体验问题；进入迭代队列。

## 5. 产品验收标准

### 5.1 核心功能验收矩阵

| 编号 | 模块 | 验收条件 | 验收证据 | 等级 |
|---|---|---|---|---|
| PRJ-001 | 项目管理 | 可新建、重命名、打开、删除本地项目；删除前二次确认 | E2E 录屏、项目目录检查 | P0 |
| PRJ-002 | 数据恢复 | 关闭并重新打开应用后，原稿、版本、评分、分镜和提示词一致 | 重启前后数据快照比对 | P0 |
| INP-001 | 创意输入 | 一句话创意可指定题材、受众、集数、单集时长、风格并生成可编辑剧本 | 固定样例生成结果 | P0 |
| INP-002 | 剧本导入 | 支持粘贴、`.txt`、`.md`；中文不乱码；原文保留 | 导入文件哈希/文本比对 | P0 |
| ANA-001 | 结构化解析 | 自动识别集、场、角色、对白、动作、旁白；无法确定的内容标为待确认 | 解析 JSON 与界面展示 | P0 |
| SCR-001 | 诊断评分 | 至少包含钩子、冲突、人物动机、节奏、反转、对白、画面化、连续性八项 | 报告截图和 JSON | P0 |
| SCR-002 | 评分解释 | 每项有分数、权重、原因、引用段落和改进建议，并展示适用目标 | 报告与原文定位 | P0 |
| SCR-003 | 评分稳定性 | 相同输入和配置可复现同一结构；模型波动时需标记模型和提示词版本 | 两次运行记录 | P1 |
| RWT-001 | 可控改写 | 可选择全文、单集、场次或段落；可输入用户提示词 | E2E 操作记录 | P0 |
| RWT-002 | 提示词合并 | 内部策略、用户提示词和故事圣经按明确优先级生效；冲突可解释 | Prompt trace 脱敏记录 | P0 |
| RWT-003 | 改写保护 | 锁定的人名、关系、世界观和核心事实默认保留；重大变更单独列出 | 改写前后差异报告 | P0 |
| VER-001 | 版本管理 | 每次 AI 改写产生独立版本；支持查看差异、命名、恢复 | 版本列表及恢复结果 | P0 |
| SHOT-001 | 分镜拆解 | 输出集、场、镜头号、景别、机位/运动、动作、对白/旁白、音效、时长 | 分镜表 JSON/Markdown | P0 |
| SHOT-002 | 时长约束 | 分镜总时长与目标单集时长偏差不超过 15%；超出时阻止静默保存并提示 | 时长校验报告 | P1 |
| SHOT-003 | 连续性检查 | 检查角色、服装/身份、场景、时间、道具和对白归属矛盾，定位到镜头 | 检查报告及问题位置 | P0 |
| AST-001 | 资产提取 | 自动提取角色、场景、道具清单；可编辑、合并、删除和锁定 | 资产清单 | P1 |
| PRM-001 | 提示词提取 | 角色、场景、道具、镜头提示词包含主体、风格、构图/镜头、动作、环境、光线 | 提示词字段检查 | P0 |
| EXP-001 | 导出 | 支持复制和导出 `.md`、`.json`；镜头提示词保留镜头号 | 导出文件解析与回读 | P0 |
| MOD-001 | Ollama | 可检测服务、列出模型、选择模型并完成生成；未启动时给出可执行提示 | 本机测试记录 | P0 |
| MOD-002 | 自定义 API | 支持 OpenAI-compatible 地址、模型名、API Key 和连接测试 | 脱敏配置与请求记录 | P0 |
| MOD-003 | 切换模型 | 切换 Ollama/API 不丢项目内容；生成记录标注模型和配置类型 | 切换前后快照 | P0 |
| ERR-001 | 失败恢复 | 超时、中断、上下文超限、鉴权失败时保留当前内容，支持重试或切换模型 | 故障注入记录 | P0 |
| SEC-001 | 密钥安全 | API Key 不出现在项目文件、导出文件、普通日志和错误提示 | 文件扫描、日志扫描 | S0 |
| OFF-001 | 本地离线 | Ollama 模式下编辑、浏览、版本恢复和导出不依赖网络 | 断网测试 | P0 |

### 5.2 质量与性能门槛

- 支持 10,000 字以内中文剧本完成导入、解析、诊断和改写，不崩溃、不静默截断。
- 所有 AI 结果均可编辑、删除、取消和重新生成；生成期间展示进度和取消入口。
- 常规界面操作在本机基准环境下 300ms 内反馈状态；模型调用展示进行中状态，不以空白界面等待。
- 核心测试用例通过率不低于 95%；P0 用例通过率必须为 100%。
- 发现 S0/S1 缺陷时，验收结论为“不通过”。

## 6. 开发标准

### 6.1 推荐技术基线

若项目尚未确定技术栈，默认采用：

- 桌面壳：Tauri。
- 前端：React + TypeScript，启用严格类型检查。
- 本地数据：SQLite，使用迁移脚本管理 schema。
- 模型接口：统一 `ModelAdapter`，分别实现 `OllamaAdapter` 和 `OpenAICompatibleAdapter`。
- 数据交换：内部使用版本化 JSON Schema，导出格式保持向后兼容。

技术栈可以替换，但必须保留以下边界：界面层、应用编排层、领域模型层、模型适配层和存储层不可互相越权调用。

### 6.2 数据与领域标准

- 所有项目对象必须有稳定 ID、创建时间、更新时间和 schema 版本。
- 原稿永远只读保存为原始输入；编辑和 AI 修改只能产生新版本。
- 角色、场景、道具和剧情事实使用结构化字段保存，不把唯一数据只放在 Markdown 中。
- 每个 AI 结果记录模型名、提供方、提示词版本、参数、输入版本 ID 和生成时间；日志中不得记录密钥。
- 数据库迁移必须可升级、可回滚或提供明确备份恢复方案。
- 删除采用软删除或回收站策略；永久删除必须经过二次确认。

### 6.3 模型调用标准

- 所有任务优先要求模型输出符合 JSON Schema 的结构化结果，再渲染为 Markdown 或表格。
- Schema 校验失败时最多自动修复/重试 2 次；仍失败则保留原内容并展示错误原因。
- 提示词分为内部策略、项目故事圣经、任务指令、用户提示词和输出格式五层，并固定优先级。
- 内部策略和用户提示词必须可追踪到版本号；禁止把用户输入直接拼接进系统级安全规则。
- 长文本采用分层处理：全文解析 -> 故事圣经 -> 按集/场/镜头生成 -> 跨层校验，不得默认一次发送整部剧本。
- 模型温度、最大输出长度、超时和重试次数必须可配置且写入生成记录。
- 用户 API 的请求失败必须区分网络、鉴权、额度、上下文和服务端错误。

### 6.4 工程质量标准

- TypeScript 不允许隐式 `any`；公共函数必须声明输入输出类型。
- 每个模块只负责一个领域职责；禁止在 UI 组件中直接拼接 SQL 或调用模型供应商 SDK。
- 所有异步任务必须支持取消、超时和错误边界；不得留下未处理的 Promise rejection。
- 关键业务规则写成纯函数，便于单元测试；随机性必须可通过 seed 或参数记录复现。
- 提交信息使用统一格式，例如 `feat: add screenplay scoring schema`、`fix: preserve locked facts`。
- 代码合并前必须通过格式化、静态检查、单元测试、集成测试和构建。

### 6.5 安全与隐私标准

- API Key 使用操作系统安全存储或本地加密存储；界面默认掩码显示。
- 项目导出、诊断报告、崩溃日志和遥测（如有）均不得包含 API Key。
- 本地模式默认不上传项目内容；任何联网请求必须由用户选择的 API 配置触发。
- 外部模型返回的内容视为不可信数据，不能直接执行为代码或文件路径。
- 导入文件限制大小、扩展名和编码；导出路径需防止覆盖非项目文件。

## 7. 开发流程与质量门禁

### 阶段 0：需求冻结

输入：用户场景和本文验收矩阵。

输出：需求编号、非目标、优先级、样例剧本、验收测试草案。

门禁：每条 P0/P1 需求都有明确输入、操作、预期结果和证据；冲突需求必须解决后再开发。

### 阶段 1：领域与交互设计

输出：项目数据模型、JSON Schema、模型适配接口、页面流程、错误状态、导入导出格式。

门禁：可从原稿版本追溯到评分、改写、分镜和提示词；故事圣经的锁定和变更规则已定义。

### 阶段 2：基础能力

实现顺序：本地项目存储 -> 原稿/版本管理 -> 模型适配 -> 结构化解析 -> 错误恢复。

门禁：无模型时仍可创建项目、编辑、保存、恢复和导出；数据库迁移和备份恢复测试通过。

### 阶段 3：核心纵向流程

按可运行切片交付：

1. 导入/创意输入 -> 解析 -> 保存。
2. 解析 -> 评分报告 -> 段落定位。
3. 评分 -> 受控改写 -> 差异与版本恢复。
4. 定稿 -> 分镜 -> 连续性检查。
5. 资产提取 -> 提示词 -> Markdown/JSON 导出。

每个切片必须包含界面、领域逻辑、持久化、错误处理和自动化测试，不接受只有模型 Demo 的“半功能”。

### 阶段 4：模型质量评测

- 建立至少 20 份脱敏剧本样例，覆盖一句话创意、短剧、长文本、对白密集、动作密集和故意存在逻辑问题的文本。
- 为每份样例准备人工基准：关键人物、事实锁定项、预期问题、合理分镜范围和不可接受改写。
- 对 Ollama 和至少一种自定义 API 分别执行同一套评测，记录结构化通过率、事实保留率、连续性问题召回率和平均耗时。
- 任何模型不能满足 Schema 或安全约束时，产品仍必须给出可恢复失败状态。

门禁：P0 样例全部可完成主流程；事实保留和密钥安全无 S0/S1 缺陷。

### 阶段 5：系统测试与发布候选

- 执行功能、回归、异常、离线、长文本、性能和安全扫描。
- 在干净环境安装并运行，验证首次配置、Ollama 未安装、Ollama 未启动、API Key 错误等场景。
- 生成发布候选版本，冻结 Schema、提示词版本和已知问题清单。

门禁：P0 100% 通过，整体不低于 95%，无 S0/S1；产品负责人和技术负责人共同签署验收单。

### 阶段 6：发布与迭代

- 发布包包含版本号、变更记录、最低系统要求、模型配置说明、备份/恢复说明和已知问题。
- 收集失败请求的脱敏统计，不默认收集剧本正文。
- 每个线上问题回填需求编号、复现样例、回归测试和修复版本。
- 每次新增模型、提示词策略或 Schema 字段都必须重新运行基准样例。

## 8. 测试标准

### 8.1 测试层级

- **单元测试**：评分权重、时长计算、事实锁定、版本差异、导出格式、错误分类。
- **契约测试**：Ollama 和 OpenAI-compatible API 的请求/响应映射、Schema 校验、超时和取消。
- **集成测试**：数据库迁移、项目保存恢复、模型结果落库、导入导出回读。
- **端到端测试**：按 `INP -> ANA -> SCR -> RWT -> VER -> SHOT -> PRM -> EXP` 跑通主链路。
- **AI 评测**：固定样例、人工基准、事实保留和连续性检查；不得只用“看起来不错”作结论。

### 8.2 每个验收用例的最低格式

```text
用例编号：SCR-002
前置条件：项目已导入中文剧本，模型连接测试通过
操作步骤：运行评分，打开“冲突”维度，点击引用段落
预期结果：显示分数、权重、原因、原文引用和改进建议，并定位到原稿
证据：截图 + 评分 JSON + 测试运行编号
结果：通过/失败
缺陷：关联 S0-S3 缺陷编号
```

## 9. Definition of Ready / Definition of Done

### 9.1 开发开始前（Ready）

- 需求编号、优先级和验收条件已确定。
- 页面状态、错误状态和空状态已设计。
- 输入输出 Schema、持久化字段和版本兼容策略已确定。
- 至少一个正常样例和一个失败样例已准备。

### 9.2 开发完成时（Done）

- 功能实现、持久化、错误恢复和可取消行为完整。
- 单元/集成/E2E 测试已添加并通过。
- 验收矩阵已填写证据链接或文件位置。
- 文档、迁移脚本、配置说明和已知问题已更新。
- 在干净环境完成一次可复现验证。

## 10. 验收执行与签署

1. 测试人员使用固定样例项目执行全部 P0 和计划内 P1 用例。
2. 对每个用例记录版本号、操作环境、模型配置类型、模型名和证据。
3. 发现问题时按 S0-S3 分级，阻断条件按本文规则执行。
4. 修复后必须补充回归用例，不能只重新点击一次确认。
5. 产品负责人确认体验和结果符合创作目标；技术负责人确认安全、数据完整性和可维护性。
6. 两方签署后，生成发布候选版本和验收报告；未通过项进入明确的后续版本，不得标记为已完成。

## 11. 发布前检查清单

- [ ] P0 用例 100% 通过，整体用例通过率 >= 95%
- [ ] 无 S0/S1 未关闭缺陷
- [ ] Ollama 正常、未启动、模型不存在三种场景已验证
- [ ] 自定义 API 正常、鉴权失败、超时、上下文超限已验证
- [ ] API Key 文件、导出物和日志扫描无泄露
- [ ] 10,000 字剧本导入、解析、改写和导出通过
- [ ] 分镜时长偏差、连续性检查和资产锁定通过
- [ ] 应用重启后数据完整，备份/恢复可用
- [ ] 干净环境安装成功，文档和已知问题清单齐全

## 12. 验收结论模板

```text
版本：
验收日期：
测试环境：
Ollama/API 配置：
P0 通过数/总数：
P1 通过数/总数：
整体通过率：
S0/S1 未关闭数：
已知问题：
验收结论：通过 / 有条件通过 / 不通过
产品负责人：
技术负责人：
```

## 13. Skill 对齐范围

本产品的 AI 创作能力必须以以下两个 Skill 作为生产规则来源，而不是只输出泛化的“大纲、分镜或提示词”：

- **小说转剧本 Skill**：将小说、章节、故事大纲或改编 brief 转为可拍摄的中文短剧分集剧本。
- **短剧制作流水线 Skill**：将分集剧本继续转为资产提取、资产板提示词、Seedance2 10 秒镜头组和四宫格剧情参考图提示词。

产品提供四种工作模式，模式可独立运行，也可按全流程串联：

| 模式 | 输入 | 必须产物 | 主要验收目标 |
|---|---|---|---|
| A：剧本改编 | 小说/章节/大纲/改编 brief/模板 | 分集剧本 Markdown | 忠实来源、可拍摄、短剧节奏、集尾钩子 |
| B：资产提取 | 已确认分集剧本、人物/风格资料 | 人物/场景/道具/UI 资产清单与提示词 | 可见且复用的资产完整，命名统一 |
| C：资产板提示词 | 资产清单、本地 PNG 参考（可选） | 角色板、场景板、道具板、UI 板提示词 | 每个资产单独可复制生成，拆解充分 |
| D：镜头组生产 | 已确认分集剧本、资产资料 | Seedance2 10 秒镜头组、四宫格提示词、视频提示词 | 镜头承接、对白、动作、声音和资产连续 |

## 14. 小说转分集剧本验收标准

### 14.1 固定 Markdown 结构

剧本产物必须按以下顺序输出，用户提供更严格模板时以用户模板为准：

1. `【本次改编】`：原文字数、改编集数、分集依据。
2. `【剧本信息】`：题材、元素、剧集篇幅、一句话简介、对标作品。
3. `【主要人设】`：角色名、年龄/身份、外表形象、性格特点。
4. `【故事梗概】`：世界观、势力/能力体系、故事背景、故事大纲。
5. `## 第X集`：每集包含 2-4 个场次（除非源结构明确要求其他数量）。
6. `【第X集 改编处理】`：删除、改写、压缩、伏笔处理。
7. `【第X集 质量自检】`：场景数量、动作描写、对话情绪标注率、本集字数、二卡悬念强度、上集承接。

### 14.2 剧本字段和可拍摄性

- 每个场次使用 `### X-1` 及 `**场：地点・日/夜・内/外**`、`**人：角色列表**`。
- 动作行必须有明确主体和可观察行为；不得只写抽象情绪或文学化总结。
- 每句对白标注简短表演情绪；情绪标签只能来自源文明确语气或合理的表演指导。
- 支持 `VO1/VO2/VO3/OS`，并区分当前人物内心、回忆/旁观叙述、全知旁白和画外声音。
- 系统声音必须标为独立的 `系统（OS，机械声）`，不得混入主角内心独白。
- 场次结尾可使用 `【一卡】`/`【二卡】`，集尾必须有明确冲突、反转或悬念钩子。
- 默认每集 1-2 分钟；系统必须根据字数、对白密度和动作数量给出时长估算。

### 14.3 原文保护和禁止臆造

- 生成前建立内部“原文声音台账”，逐条登记原文对白、明确内心独白以及选作 VO/OS 的原文叙述。
- 受保护台词只能拆分，不能删除、总结、改写、弱化、净化或合并；必须保留原顺序和原措辞。
- 原文未提供的道具、事件、关系、地点、背景、情绪、氛围、动作和镜头细节不得擅自增加。
- 来源信息不足时只能做最小推断，并在改编处理或待确认项中明确标记。
- 若外部安全规则阻止原文逐字输出，必须保留原位置，并输出“原文台词待人工确认”的占位说明，不能静默改写。

### 14.4 剧本模式验收用例

| 编号 | 验收条件 | 证据 | 等级 |
|---|---|---|---|
| SK-SCR-001 | 输入一章小说后可生成指定单集，不额外生成用户未要求的其他集 | 输入/输出文件、集数检查 | P0 |
| SK-SCR-002 | 固定结构、场次标题和动作/对白格式完整 | Markdown 结构解析 | P0 |
| SK-SCR-003 | 原文对白和明确内心独白逐条通过声音台账比对 | 台账差异报告 | P0 |
| SK-SCR-004 | 生成内容不出现来源外新增人物、道具、地点或事件 | 人工基准 + 差异报告 | P0 |
| SK-SCR-005 | 每集有独立冲突、一次反转、悬念点和集尾钩子 | 质量自检 + 人工验收 | P0 |
| SK-SCR-006 | 改编处理解释删除、改写、压缩和伏笔选择；自检报告给出计数 | 处理说明与自检块 | P1 |

### 14.5 影视化语言兼容规则

当用户选择“小说转剧本”模式且未提供更严格模板时，剧本 Markdown 顶部必须先输出以下元数据块，再进入剧本正文：

```text
剧名：
题材：
画风：
剧集篇幅：
总集数：
总时长：
```

元数据后固定包含：

```text
## 旁白与画外音规则
## 人物动作规则
## 一、剧情大纲
## 二、人物小传
## 三、正文
```

同时满足以下标签规则：

- `VO1` 表示当前时刻人物内心，`VO2` 表示回忆/后时叙述，`VO3` 表示全知客观旁白，`OS` 表示不在画面中的声音或画外心理活动。
- `△` 只用于有明确主体的动作、表情变化、摄像机运动或由主体造成的视觉变化。
- `【画面】` 用于静态画面、环境、氛围和无主体可见状态；`【音效】` 用于有来源或有戏剧功能的声音。
- 非对白动作和画面描述使用角色名或明确主体；第一人称只保留在对白、VO 或 OS 中。
- 不使用“突然、似乎、感觉、情绪蔓延”等无法直接拍摄的抽象描述，改用可观察动作、画面、声音或屏幕文字表达。

对应验收用例：

| 编号 | 验收条件 | 证据 | 等级 |
|---|---|---|---|
| SK-SCR-007 | 剧本元数据和五个固定正文章节顺序正确 | Markdown AST 检查 | P0 |
| SK-SCR-008 | VO1/VO2/VO3/OS 和系统 OS 标签使用正确，无混淆主角内心与系统声音 | 标签规则扫描 + 人工抽查 | P0 |
| SK-SCR-009 | 动作、画面、音效标签均符合主体和可拍摄性规则 | 规则扫描 + 场次评审 | P1 |

## 15. 资产提取与统一命名验收标准

### 15.1 提取范围

系统只提取画面中可见或在生产中重复使用的资产，并固定分为四类：

- 人物：角色、群演身份、重复出现的关键人物。
- 场景：地点、内外景、空间结构和重复场景。
- 道具：推动剧情的物件、文件、符箓、玉简、徽章、键盘等可读物件。
- UI：系统弹窗、任务面板、人物信息面板、应用弹窗和其他可读界面。

### 15.2 命名规则

- 人物统一使用 `@人物名`。
- 场景统一使用 `@场景名`。
- 道具统一使用 `@道具名`。
- UI 统一使用 `@UI名`。
- 同一实体在剧本、资产清单、资产板和镜头提示词中不得改名。
- 后续镜头反复出现的道具或 UI 必须先在资产清单中建立一等资产条目。

### 15.3 资产提取验收用例

| 编号 | 验收条件 | 证据 | 等级 |
|---|---|---|---|
| SK-AST-001 | 从剧本提取四类资产，并标记首次出现的集/场/镜头 | 资产清单与来源定位 | P0 |
| SK-AST-002 | 所有镜头中使用的重复资产均可在资产清单中找到 | 名称交叉引用报告 | P0 |
| SK-AST-003 | 资产支持编辑、合并、删除和锁定，修改后下游提示词可重新生成 | E2E 操作记录 | P1 |
| SK-AST-004 | 可读文档、符箓、招牌、玉简、徽章和 UI 不被错误归为普通背景 | 人工基准比对 | P1 |

## 16. 资产板提示词验收标准

### 16.1 单资产自包含要求

每个资产条目必须可以脱离全局说明单独复制使用，并包含：

- `用途`
- `风格基线`
- `拆分细节`
- 适用时的 `色卡约束`
- `版式要求`
- `提示词`
- `避免`

### 16.2 板式和分解要求

- 默认输出 `16:9`、`2K`、中文标签、无随机英文字母和无随机符号。
- 角色板包含主视英雄图、正/侧/背三视图、表情组、服装层次、材质细节、配饰组和携带/固定道具；背视图必须展示发型后部、轮廓、挂件、服装层次和鞋型。
- 场景板包含主场景图、结构/布局视图、多角度格、氛围研究、必要时的路线/机位参考，以及主色/辅助色/点缀色/饱和度/对比度/光线类型/禁止偏色方向色卡。
- 道具板包含正/侧/背三视图，适用时补充开合、启用/未启用状态、材质近景和结构细节。
- UI 板包含组件网格和可读中文示例，不出现随机符号或未授权英文。
- 有本地 PNG 参考时，板式语言应与参考图一致，不能另起一套布局。

### 16.3 资产板验收用例

| 编号 | 验收条件 | 证据 | 等级 |
|---|---|---|---|
| SK-BOARD-001 | 每个资产条目均具备七个自包含字段 | 字段完整性检查 | P0 |
| SK-BOARD-002 | 角色、场景、道具、UI 分别满足对应分解要求 | 资产板提示词评审 | P0 |
| SK-BOARD-003 | 场景之间的色卡约束可复用且没有随机偏色 | 色卡对比表 | P1 |
| SK-BOARD-004 | 单独复制任一资产块即可提交至图像模型生成 | 复制测试与模型调用记录 | P0 |

## 17. Seedance2 镜头组与四宫格验收标准

### 17.1 镜头组固定结构

默认以约 10 秒为一组，每组按以下顺序输出：

1. `【人物信息】`
2. `【场景信息】`
3. `【道具与UI信息】`
4. `【对话锁定】`
5. `【分镜承接】`
6. `【风格总纲】`
7. 3-4 个定时镜头块
8. `【本组剧情四宫格参考图提示词（含图片提示词）】`
9. `【本组视频提示词】`

每个定时镜头必须包含时间范围、镜头 ID、转场、镜头/镜头运动、主体可见动作、音效和旁白/对白。默认不添加 BGM 或背景音乐描述。

### 17.2 承接与四宫格一致性

- `【分镜承接】` 必须说明前一镜头末尾画面、本组开场画面、承接逻辑和剪辑意图。
- 四宫格和定时镜头使用同一组 shot ID、场景名、镜头规格和核心动作。
- 每个四宫格格子包含场景名、镜头规格、画面描述、可直接复制的图片提示词和一行色调脚本。
- 若用户指定“先四宫格，后视频提示词”，该顺序必须在所有镜头组中保持一致。
- 视频提示词不得使用未命名且会重复出现的道具或 UI；所有重复实体必须引用 `@` 资产名。

### 17.3 统一风格约束

除非用户明确修改，流水线各阶段保持：中文仙侠 3D 动画视觉、非真人实拍人物、稳定角色身份、统一道具/场景/UI 命名、中文可读标签、无随机额外文字。

镜头输出默认携带：禁止字幕、禁止 BGM 描述、面部不变形、动作自然连贯、无闪烁重影、虚拟摄像机平稳流畅。

### 17.4 镜头组验收用例

| 编号 | 验收条件 | 证据 | 等级 |
|---|---|---|---|
| SK-SHOT-001 | 每组约 10 秒且包含 3-4 个定时镜头，字段齐全 | 模板结构解析 | P0 |
| SK-SHOT-002 | 角色、场景、道具/UI、对白和风格总纲均与确认剧本一致 | 交叉引用报告 | P0 |
| SK-SHOT-003 | 前后镜头承接信息明确，动作和转场可执行 | 人工镜头评审 | P0 |
| SK-SHOT-004 | 四宫格与 timed shots 的 ID、场景、镜头和动作逐项一致 | 自动比对报告 | P0 |
| SK-SHOT-005 | 视频提示词包含默认负面约束且无字幕/BGM 违规描述 | 规则扫描 | P0 |

## 18. Skill 对齐后的端到端生产流程

### 阶段 A：源材料接收

收集小说/章节/大纲、用户模板、题材、目标受众、总集数、单集时长和风格要求。系统生成源材料摘要、人物候选、地点候选、时间线和待确认项；不得在此阶段生成未经确认的剧情事实。

### 阶段 B：分集策略与剧本改编

先确定分集依据、每集独立冲突、反转和集尾钩子，再生成目标集数的剧本。生成前建立原文声音台账，生成后执行格式、来源忠实度、对白保护、禁止臆造和单集质量自检。

### 阶段 C：剧本确认与故事圣经锁定

用户确认剧本版本后，系统提取并展示人物、场景、道具、UI、时间线和锁定事实。只有确认或锁定后的剧本才能进入资产板和镜头组生产；任何锁定项变更必须产生版本差异和影响范围。

### 阶段 D：资产提取与资产板提示词

按 `@人物/@场景/@道具/@UI` 建立资产清单，检查镜头引用是否完整，再逐资产生成自包含板提示词。使用本地 PNG 时记录参考文件路径、尺寸和采用的板式规则。

### 阶段 E：Seedance2 镜头组

按场次拆为约 10 秒组，先编写人物/场景/道具 UI/对白锁定/承接/风格总纲，再生成 timed shots、四宫格参考图提示词和视频提示词。完成后自动执行 ID、命名、对白、时长、资产和默认约束扫描。

### 阶段 F：导出与回读验收

按以下顺序提供产物：

1. 分集剧本 Markdown
2. 资产提取与提示词 Markdown
3. 资产板提示词 Markdown
4. Seedance2 镜头组 Markdown

同时导出 JSON 供应用回读。回读后必须保持集、场、镜头、资产 ID 和版本关系不变；Markdown 是人类生产交付物，JSON 是系统交换和回归测试基准。

## 19. Skill 对齐后的发布门禁补充

- [ ] 至少一份小说/章节样例完整跑通 A-F 六个阶段
- [ ] 原文声音台账与最终对白逐条比对，无静默删除或改写
- [ ] 剧本不存在来源外新增事实，新增推断均已标记待确认
- [ ] 四类资产和 `@` 命名在剧本、资产板、镜头组之间零断链
- [ ] 每个资产块可独立复制；角色/场景/道具/UI 板分解要求通过
- [ ] 每个 Seedance2 镜头组约 10 秒，四宫格与视频提示词 ID 对齐
- [ ] 默认“无字幕、无 BGM、稳定身份、平稳镜头”规则扫描通过
- [ ] 四种 Markdown 产物和 JSON 回读通过
